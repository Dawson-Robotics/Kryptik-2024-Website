{
  "name": "kryptik-content-api",
  "version": "1.0.0",
  "description": "All website content will be written in **Markdown** which is a simple formatting style for text that allows plenty of control on how articles look.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon bin/www-dev",
    "start": "node bin/www-dev",
    "build": "npm i && cd ../../site/ && npm i && npm run build && ./finishbuild.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "https": "^1.0.0",
    "path": "^0.12.7"
  }
}
